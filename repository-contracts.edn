{:canonical {:format :edn :manifest "manifest.edn" :schema "schema.edn" :lexicons "lex"}
 :wire {:roots ["wire" ".well-known"] :formats #{:json :jsonld :bpmn}}
 :forbidden-root-paths ["manifest.jsonld"] :deprecated-runtimes #{:go :tinygo :shell}
 :legacy-root "20-actors/shirabe"}
